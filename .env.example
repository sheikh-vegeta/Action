# --- LLM Configuration ---
# Set the provider to "openrouter" or "openai"
LLM_PROVIDER=openrouter
# Set the model ID to use. For OpenRouter, find models at https://openrouter.ai/models
MODEL_ID=anthropic/claude-3-sonnet:free

# --- API Keys ---
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key
# Or, if using OpenAI directly:
OPENAI_API_KEY=your_openai_api_key

# --- Docker Compose ---
# This is used by docker-compose to name the network.
# You usually don't need to change this.
COMPOSE_PROJECT_NAME=agent
